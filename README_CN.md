# 印迹 (PhotoStamp)

跨平台批量照片水印工具，基于 Electron 构建，支持 macOS / Windows / Linux。

[English](./README.md)

![Electron](https://img.shields.io/badge/Electron-40-47848F?logo=electron)
![Platform](https://img.shields.io/badge/Platform-macOS%20%7C%20Windows%20%7C%20Linux-blue)

## 功能特性

### 水印内容

- **拍摄时间** — 自动读取 EXIF 信息标注拍摄日期和时间，提供 6 种预设格式及自定义模式（支持 YYYY/MM/DD/HH/mm/ss 占位符）
- **拍摄地点** — 根据 GPS 坐标反向地理编码，支持 3 种显示模式：
  - 地理位置名称（市级 / 区级 / 街道三级精度）
  - GPS 坐标
  - 自定义位置文本
- **地点选项** — 可自定义位置前缀（默认📍），可隐藏省/州信息，支持根据本国设置自动切换地图服务商
- **宝宝年龄** — 设定出生日期后自动计算拍摄时的年龄，支持自定义前缀文字
- **自定义文本** — 添加任意自定义水印文字

### 水印样式

- **字体** — 系统字体选择，跨平台自动发现字体
- **加粗 / 斜体** — 切换字体加粗和斜体样式
- **颜色与透明度** — 完整取色器，透明度可调
- **字号** — 自动大小或手动像素设定
- **描边** — 可调描边粗细和颜色
- **阴影** — 无 / 轻 / 中 / 强四档阴影预设
- **位置** — 左上、右上、居中、左下、右下五个位置
- **文本对齐** — 左对齐 / 居中 / 右对齐

### 处理与输出

- **批量处理** — 一次选择多张照片，批量添加水印
- **拖放导入** — 直接拖放照片到窗口导入，支持追加模式并自动去重
- **实时预览** — 调整参数后即时预览水印效果，支持缩放和拖拽
- **多格式输出** — 支持 JPEG / PNG / WebP，可调节输出质量
- **完成后打开文件夹** — 可选配置，处理完成后自动打开输出目录
- **覆盖检测** — 输出时自动检测已有文件，可选覆盖或跳过
- **进度与通知** — 批量处理时在任务栏显示进度条，完成后发送系统通知

### 应用

- **多语言** — 支持中文 / English
- **深色模式** — 支持亮色 / 暗色 / 跟随系统三种主题
- **API Key 安全** — 使用 Electron safeStorage 加密存储 API 密钥，密钥不离开主进程
- **API Key 测试与用量** — 每个服务商可在线测试连通性，一键跳转查看用量
- **设置自动持久化** — 所有水印配置和 UI 状态自动保存和恢复

## 支持的图片格式

JPG / JPEG / PNG / HEIC / TIFF / WebP

## 地图服务商

| 分类 | 服务商 |
|------|--------|
| 全球 | Google Maps、Mapbox、MapTiler |
| 中国 | 高德地图、腾讯位置服务、天地图、和风天气 |

支持设置本国/非本国自动切换服务商（支持 16 个国家/地区）。

## 快速开始

### 环境要求

- Node.js >= 18
- npm

### 安装依赖

```bash
npm install
```

### 开发运行

```bash
npm start
# 或开启 debug 日志
npm run dev
```

### 构建打包

```bash
# macOS
npm run build:mac

# Windows
npm run build:win

# Linux
npm run build:linux

# 全平台
npm run build
```

构建产物位于 `dist/` 目录。

## 项目结构

```
photostamp/
├── main.js              # Electron 主进程
├── preload.js           # 预加载脚本 (contextBridge)
├── lib/
│   ├── exif.js          # EXIF 信息读取
│   ├── fonts.js         # 系统字体获取
│   ├── fontconfig.js    # Fontconfig 初始化
│   ├── geocoder.js      # 反向地理编码 (多服务商)
│   ├── logger.js        # 日志模块
│   └── watermark.js     # 水印处理核心 (sharp + Pango)
├── renderer/
│   ├── index.html       # 主界面
│   ├── styles.css       # 样式
│   ├── app.js           # 渲染进程逻辑
│   └── i18n.js          # 国际化
├── assets/              # 图标资源
├── scripts/
│   └── obfuscate.js     # 构建时 JS 混淆
├── package.json
└── dist/                # 构建产物
```

## 技术栈

- **Electron** — 跨平台桌面应用框架
- **sharp** — 高性能图片处理（libvips + Pango 文字渲染）
- **ExifReader** — EXIF 元数据读取

## License

MIT
